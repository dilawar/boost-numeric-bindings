How to obtain a new version:
svn co http://svn.boost.org/svn/boost/sandbox/boost/numeric/bindings

Make a patch:
diff -x '.svn' -x '.git' -x '*.orig' -x '*.patch' -x '*~' -wruN . ~/tmp/boost-sandbox/
